import {Box} from "@material-ui/core";

export const Footer = () => {
    return(
        <Box pb={20} pt={20} sx={{backgroundColor: "#444f60", textAlign: "center", color: "white"}}>
            This is a footer
        </Box>
    )
}